#include <stdio.h>

// write after the placeholder to include more text - don't open another set of text after the variable

int main(){
    char name[]="Aamir";
    printf("Hello %s!\n", name); // %s is the placeholder for char
    return 0;
}